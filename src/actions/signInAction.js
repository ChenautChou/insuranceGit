import {axios} from 'axios';



//
export function signIn() {
    return axios.post('',{},)
        .then((res) => {
            return res;
        })
        .catch(() => {

    })
}

export function getUserInfo() {
    return axios.post('',{},)
        .then((res) => {
            return res;
        })
        .catch(() => {

        })
}